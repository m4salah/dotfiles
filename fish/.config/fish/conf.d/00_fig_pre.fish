# Disabled because Kiro's shell integration adds substantial work before the
# first prompt. Uncomment to restore Kiro terminal integration.
# test -x ~/.local/bin/kiro-cli; and eval (~/.local/bin/kiro-cli init fish pre --rcfile 00_fig_pre | string split0)
