# Disabled because the generated hook enumerates every Fish alias before the
# first prompt. Uncomment to restore Kiro terminal integration.
# test -x ~/.local/bin/kiro-cli; and eval (~/.local/bin/kiro-cli init fish post --rcfile 99_fig_post | string split0)
